# Mux API credentials — create a token at https://dashboard.mux.com/settings/access-tokens
# Without these, the app runs in "demo mode" using the curated seed catalogue.
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
